# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api/me/me-unauthorized.spec.ts >> GET /me trả về 401 khi chưa có session cookie
- Location: tests/api/me/me-unauthorized.spec.ts:3:5

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 401
Received: 525
```

# Test source

```ts
  1 | import { expect, test } from '../../../src/fixtures/api.fixture';
  2 | 
  3 | test('GET /me trả về 401 khi chưa có session cookie', async ({ meService }) => {
  4 |     const response = await meService.profile.getMe();
> 5 |     expect(response.status()).toBe(401);
    |                               ^ Error: expect(received).toBe(expected) // Object.is equality
  6 |   });
  7 |   
```